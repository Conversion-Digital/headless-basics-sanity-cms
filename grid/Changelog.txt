## 2025-03-10
- Updated handleItemChange in SanityGrid.tsx to retrieve sub-members of the `component` field.

## 2025-03-10 (second update)
- Revised handleItemChange to properly match sub-items in the `component` array by `_type` and attach them to `_componentMembers`.

## 2025-03-10 (schema lookup update)
- Changed handleItemChange to directly fetch the relevant schema fields for `_componenttype` from the schema definition, rather than relying on form members.

## 2025-03-10 (circular structure fix)
- Now storing only minimal, non-circular field info in `_componentMembers` and removing it before patching to avoid JSON.stringify errors.